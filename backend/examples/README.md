# Example JSON

| File | Description |
|------|-------------|
| `sample-ai-output.json` | LLM-style response before server assigns `id` fields |
| `sample-canvas-state.json` | Validated state with `id` and `updatedAt` |
